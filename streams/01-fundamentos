// NO NODE TODA PORTA DE ENTRADA E SAÍDA AUTOMATICAMENTE É UMA STREAMS

// STREAM READABLE processo para ler arquivos aos poucos

// EXEMPLO

// Importação de Clientes via CSV (Excel)
// 1gb - 1.000.000 (1 gb com 1mi de linhas)
// POST /upload import.csv (caminho)

// 10mb/s - 100s (tempo para subir o arquivo sem streams)

// 100s -> para fazer inserções no banco de dados

// COM STREAMS
// 10mb/s -> 10.000 (em 10mb/s teriamos 10k linhas)



// STREAM WRITABLE processo para enviar arquivos aos poucos

// STREAM TRANSFORM processo para transformar um arquivo readable e envia-lo diferente para o writable